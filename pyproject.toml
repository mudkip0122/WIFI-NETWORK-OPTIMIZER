[project]
name = "wifi-network-optimizer"
version = "0.1.0"
description = "Wi-Fi network quality analysis and optimization system"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
visualizer = ["matplotlib>=3.10,<4", "numpy>=2.2,<3"]

[tool.ruff]
target-version = "py313"
line-length = 100
