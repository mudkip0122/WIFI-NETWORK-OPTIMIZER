# 검증 코드

`python -m unittest discover -s tests -v`로 자동 테스트를 실행한다.
언어별 Wi-Fi 파싱, RSSI 출처, Ping 통계·오류, 속도 단위·오류, 통합 결과를 검증한다.
자동 테스트는 네트워크를 실제로 사용하지 않으며 실환경 검증 결과는 주차별 문서에 기록한다.
개발 환경 확인은 `python -m wifi_optimizer --check`로 실행한다.
